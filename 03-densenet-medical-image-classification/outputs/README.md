# Output Artifacts

The PNG plots in this folder were extracted from the outputs embedded in the attached executed notebook.
CSV and JSON files reproduce the numeric evidence recorded in that notebook.

Because the executed experiment used a Fashion-MNIST-derived proxy task, these artifacts must not be
presented as clinical pneumonia-detection results.

The Kaggle notebook will regenerate a separate full evaluation package for a real chest-X-ray dataset,
including classification report, confusion matrix, ROC curve, precision-recall curve, threshold analysis,
sample predictions, error analysis, and Grad-CAM examples.

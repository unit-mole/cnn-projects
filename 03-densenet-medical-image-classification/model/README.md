# Generated TensorFlow.js model

The GitHub Actions workflow converts `models/densenet121_medical_browser.h5` into TensorFlow.js LayersModel files in this folder immediately before deployment.

Generated `model.json` and `.bin` weight shards are intentionally not committed to `main`; they are published to the `gh-pages` branch under `03-densenet-medical-image-classification/model/`.

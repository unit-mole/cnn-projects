# Outputs

- `figures/` contains plots extracted from the executed source notebook.
- `metrics/` contains the supplied JSON metrics and baseline comparison table.
- `predictions/` is reserved for exported prediction summaries.
- `runtime/` receives temporary masks generated by the Gradio app and is ignored by Git except for `.gitkeep`.

# Streamlit Note

This project intentionally uses Vercel + TensorFlow.js as its primary deployment. The directory is retained only to preserve consistency with earlier portfolio layouts; no Streamlit runtime is required.

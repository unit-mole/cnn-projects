# Raw data

The default CIFAR-10 dataset is downloaded automatically. For a custom dataset, place files under `data/raw/custom_dataset/` using one subfolder per class. Raw data are intentionally ignored by Git.

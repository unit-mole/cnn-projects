# Dataset notes

The default training source is CIFAR-10, downloaded by TensorFlow at runtime. Do not commit the full dataset.

For a custom folder dataset, use one folder per class or a `train/` and `test/` structure. Only redistribute images when licensing permits it. Remove private information and EXIF metadata from public samples.

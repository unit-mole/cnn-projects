# Supplied neural-style-transfer source

This archive contains the original uploaded notebook and outputs. The notebook uses VGG19 features to combine a CIFAR-10 content image with a synthetic style image. It is retained for traceability only and is deliberately separated from the AlexNet-style classification implementation.

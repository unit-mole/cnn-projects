# Hosting summary

- **Primary:** Cloudflare Pages serving `web/`
- **Fallback:** GitHub Pages serving the same `web/` directory
- **Backend:** none
- **Runtime inference:** TensorFlow.js in the visitor's browser
- **Model training:** Python/TensorFlow outside page load

Read `README_CLOUDFLARE.md` and `README_GITHUB_PAGES.md` for full instructions.

# Output Policy

`outputs/metrics/model_metrics.json` and the baseline comparison chart are created from the supplied metrics artifact. Other evaluation files are generated only after running `scripts/evaluate_model.py` against real predictions. Empty placeholders are not presented as measured results.

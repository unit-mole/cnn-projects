# Monorepo Integration

Copy this folder into the root of `cnn-projects` and place the workflow at `.github/workflows/04-image-classification-resnet.yml`. Keep project dependencies, tests, notebook, model metadata, and browser source inside the numbered project folder. The workflow path filters prevent unrelated CNN projects from triggering this project's CI.

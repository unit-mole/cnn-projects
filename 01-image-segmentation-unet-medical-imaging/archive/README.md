# Archive Notes

The supplied notebook was reviewed as the source of truth for the synthetic data generator, model architecture, training settings, baseline, and evaluation metrics.

The original raw notebook is not copied into the public-ready archive because it contains machine-specific local paths and several statements that could be misread as claims about clinical data. A cleaned, portable notebook is provided in `notebooks/image_segmentation_unet_medical_imaging.ipynb`.

See `PROJECT_AUDIT.md` and `IMPROVEMENTS.md` for the exact findings and corrections.
